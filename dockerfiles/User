FROM builder:latest AS builder

WORKDIR ./services/user/cmd/user-service

RUN go build
RUN pwd; ls


FROM alpine:latest

COPY --from=builder /build/services/user/cmd/user-service /usr/local/sbin/

ENTRYPOINT ["/usr/local/sbin/user-service"]
